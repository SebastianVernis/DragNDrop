# ================================================
# SECRETS VAULT - Backup de Secrets
# ================================================
# Este archivo sirve como BACKUP de qué secrets existen
# NO contiene los valores reales (están en Cloudflare)
# 
# Para restaurar secrets, usa: ./scripts/deploy-secrets.sh

# ================================================
# SECRETS CONFIGURADOS EN CLOUDFLARE PAGES
# ================================================
# Fecha última actualización: 2025-12-02

# AI APIs
BLACKBOX_API_KEY=✓ Configurado
FALLBACK_BLACKBOX_API_KEY=✓ Configurado
BLACKBOX_ACCESS_API_TOKEN=✓ Configurado
GEMINI_API_KEY=✓ Configurado (también como VITE_ para cliente)

# Database (Pendiente - Workflow 3)
DATABASE_URL=⏳ Pendiente

# Better Auth (Pendiente - Workflow 3)
BETTER_AUTH_SECRET=⏳ Pendiente
BETTER_AUTH_URL=⏳ Pendiente

# OAuth Secrets (Pendiente - Workflow 3)
GOOGLE_CLIENT_SECRET=⏳ Pendiente
GITHUB_CLIENT_SECRET=⏳ Pendiente

# Deployment (Pendiente - Workflow 4)
VERCEL_TOKEN=⏳ Pendiente
VERCEL_CLIENT_SECRET=⏳ Pendiente

# ================================================
# VARIABLES PÚBLICAS (VITE_*)
# ================================================
# Estas NO son secretos, van en el build

VITE_GEMINI_API_KEY=✓ En .env
VITE_API_URL=⏳ Por configurar
VITE_APP_URL=https://dragndrop-editor.pages.dev
VITE_ENABLE_AI_FEATURES=true
VITE_ENABLE_CLOUD_SYNC=false
VITE_ENABLE_DEPLOYMENTS=false

# ================================================
# INSTRUCCIONES
# ================================================
# 
# Para listar secrets actuales:
#   ./scripts/list-secrets.sh
#
# Para subir todos los secrets desde .env:
#   ./scripts/deploy-secrets.sh
#
# Para subir un secret individual:
#   echo "valor" | wrangler pages secret put KEY --project-name=dragndrop-editor
#
# Para eliminar un secret:
#   ./scripts/delete-secret.sh KEY
